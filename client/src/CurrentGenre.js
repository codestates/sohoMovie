export default function CurrentGenre({ currentGenre }) {
  return (
    <div className='current-genre'>
      {/* Advanced Challenge: currentGenre props에 따라 표시되는 값이 변화되어야 합니다. */}
      <div>{'선택된 장르 : 없음'}</div>
    </div>
  );
}
